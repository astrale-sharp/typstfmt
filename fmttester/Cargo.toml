[package]
name = "fmttester"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
lexopt = "0.3.0"
similar = "2.2.1"
tempfile = "3.7.0"
typstfmt_lib = {path = "../lib/"}
